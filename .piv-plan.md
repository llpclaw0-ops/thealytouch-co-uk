# PIV PLAN — The Aly Touch site repair

## Adversarial pass (plan-feature gate)
- **KILL:** Nothing. The live site currently publishes a WRONG PHONE NUMBER and,
  on JS failure, invented customer jobs ("End-of-tenancy whole home", "Office
  refresh"). Both are active commercial/truth defects. Kill is not available.
- **RESHAPE:** Stop refining images (12 states exist, honestly labelled
  illustrative, scene-matched by construction). Spend the budget on truth +
  brand defects, which are higher harm and fully deterministic to fix.
- **PROCEED:** Reshaped. Execute truth-first, then brand, then verify.

## Current RED properties (ground truth, read from source)
1. `js/site.js:9` — phone is `01481 000 000`. Confirmed number is 07781 446239.
2. `js/site.js:63` — `#work` rebuilt via `innerHTML`. Underlying static HTML in
   `index.html` contains invented jobs + links to project-office/tenancy/
   windows/sofa. **JS failure reveals misleading fallback.** (Gate 16)
3. Homepage has 4 comparison cards, not 6, and each loads the SAME image on both
   sides (a CSS filter is the only "before"). Not a real before/after.
4. Homepage carries a SECOND competing generic service grid (`#service`).
5. Palette is dark green/blue: `--brand:#123b35`, `theme-color:#0b2341`.
6. Unsupported claims live: "free quote", "office cleaning" (incl. `<title>`),
   carpets/windows/holiday-let/tenancy services and imagery.

## Done means
- Six real before/after cards, in STATIC HTML (no JS injection), each with a
  genuinely different before and after file.
- No unsupported service or claim in source or rendered DOM.
- Beige/ivory-led palette from shared tokens.
- 07781 446239 / tel:07781446239 everywhere, no conflicting numbers.
- All 20 gates pass.

## Must stay untouched
- The Get a Quote route, the contact form markup/IDs, and its submit handler.
- The logo file (hash-verified, installed).

## Out of scope
- External deployment. Deleting the project. New quote workflow.

## Proof required
- `node --check` on JS; Chrome DOM assertions for card count, handle count,
  pointer drag delta, keyboard delta, scrollWidth, console errors; curl/grep
  for phone + banned terms across every page; local href + image existence
  sweep; contrast computation; SHA-256 logo match.

## Order (truth first)
1. site.js — phone, delete fallback injection, keep sliders.
2. index.html — six static cards, remove duplicate grid, strip claims.
3. style.css — palette tokens, typography, comparison component, spacing.
4. services.html — same six groups + anchors.
5. Remaining pages incl. legacy project-*/post-* pages.
6. Verification gates.
